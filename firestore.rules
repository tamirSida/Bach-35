rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all drills
    match /drills/{document} {
      allow read: if true;
      allow write, update, delete: if request.auth != null;
    }
  }
}